# CPU Task Scheduler App

A zero-compile browser simulator for Look-Ahead Conserving EDF scheduling. It is built as a self-contained static web app with plain HTML, CSS, and vanilla JavaScript ES modules.

## Run

Open `index.html` in a modern browser.

No install, build, package manager, transpiler, or server is required for normal use.

## What It Does

- Defines periodic real-time tasks with release time, WCET, actual execution time, period, deadline, color, and enabled state.
- Defaults the simulation length to 100 time units.
- Generates job instances through a finite simulation horizon.
- Runs a Look-Ahead Conserving EDF simulation with conservative WCET reservations.
- Uses discrete P-states: `0`, `0.25`, `0.5`, `0.75`, and `1.0`.
- Reclaims slack when actual execution finishes before WCET.
- Renders an SVG schedule timeline and frequency track.
- Shows a shared CPU execution lane with per-task lanes for readability.
- Automatically recomputes when task data changes.
- Provides a combined WCET / Actual Execution Time control for each task.
- Includes a collapsible algorithm brief in the app shell.
- Reports deadline misses, idle time, preemptions, utilization, deferred work, and reclaimed slack.
- Supports built-in ECU-style scenarios, premade task selection, and JSON import/export.

## Test

The core scheduler tests are dependency-free:

```sh
node tests/scheduler.test.mjs
```

The tests exercise job generation, validation, EDF/P-state behavior, deadline miss reporting, slack reclamation, trace interval sanity, and disabled task exclusion.

## Project Rules

Agents must read `AGENTS.md` before planning or making changes. Any agent that plans or works in this repo must append a brief entry to `agents_log`.

## AI Use Disclosure

GPT Codex 5.4 was used to develop and test code generated from machine-readable development plans and general research generated by GPT 5.4 - which was prompted with novel and unique design ideas that were conceptualized by me during the design phase. The development of the app was split up over 27 commits, over the course of which I extensively steered the creative and technical direction of the project, until the outcome met the goals of my vision. Some features were not developed due to time constraints.

## Future Additions

- Comparison with other scheduling methods
- A more visually appealing task assignment process (Education Mode)
- Mathematical calculation visualization to explain how task order was calculated
- A less cluttered Task creation UI
- Refinements for CPU Trace UI, including smoother timeline scrubbing and more concise, visual, "WHICH, WHY, WHEN" justifications.
